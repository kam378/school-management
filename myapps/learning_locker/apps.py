from django.apps import AppConfig

class LearningLockerConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'myapps.learning_locker'
    verbose_name = 'Learning Locker'
